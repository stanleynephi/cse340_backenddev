<% if (accountData) { %>
    <h2>Welcome <%= accountData.account_firstname %>
    </h2>

    <!-- Update Account Link -->
    <p>
        <a href="/account/update/<%= accountData.account_id %>">Update Account Information</a>
    </p>

    <!-- Inventory Management Section for Employees/Admins -->
    <% if (accountData.account_type==="Employee" || accountData.account_type==="Admin" ) { %>
        <h3>Inventory Management</h3>
        <p>
            <a href="/inv/management">Go to Inventory Management</a>
        </p>
        <% } %>

            <% } else { %>
                <h2>You must be logged in to view this page.</h2>
                <% } %>